# Path to your Oh My Blackthunder installation (your actual clone path).
# Tip: `tools/install.sh` wires this up for you automatically.
export OMB="$HOME/.oh-my-blackthunder"

# Optional prompt theme (themes/<name>.zsh-theme).
OMB_THEME="oh-my-black"

# Enabled plugins (plugins/<name>/<name>.plugin.zsh).
#   ai-blackthunder … Claude/Codex usageをブラックサンダー本数に換算して表示
#   omb-games … ターミナルで遊べるブラックサンダー風ミニゲーム集（omb <game>）
#   cat … Black Thunder style cat（thunder_cat）
#   diff … Black Thunder style diff（thunder_diff）
#   fzf … Black Thunder style fzf（thunder_fzf）
#   log … Black Thunder style log colorizer（thunder_log）
#   ls … Black Thunder style ls（thunder_ls）
#   vim … Black Thunder style Vim launcher（thunder_vim）
plugins=(ai-blackthunder cat diff fzf log ls omb-games vim)

source "$OMB/oh-my-black.sh"
